<Window x:Class="GaSongJang.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="가송장 생성기"
        Width="1000"
        Height="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA"
        FontFamily="Segoe UI"
        ResizeMode="CanMinimize">

    <Grid Background="#FFFFFF" Margin="30">
        <StackPanel VerticalAlignment="Top">

            <!-- TITLE -->
            <Label Content="가송장 생성기"
                   FontSize="32"
                   FontWeight="Bold"
                   Foreground="#1F2937"
                   Margin="10,20,10,0"/>

            <!-- STATUS -->
            <Label x:Name="StatusLabel"
                   Content="📂 파일을 선택하세요"
                   FontSize="16"
                   Foreground="#6B7280"
                   Margin="10,5,10,0"/>

            <!-- DIVIDER -->
            <Border Height="1" Background="#E5E7EB" Margin="10,15,10,0"/>

            <!-- UPLOAD BUTTON -->
            <Button x:Name="BtnUpload"
                    Content="📂 파일 선택"
                    FontSize="14"
                    FontWeight="Bold"
                    Foreground="#1F2937"
                    Background="#F3F4F6"
                    Click="BtnUpload_Click"
                    Height="44"
                    Margin="10,20,10,0"
                    Cursor="Hand"/>

            <!-- DATAGRID -->
            <DataGrid x:Name="DgvMarkets"
                      Margin="10,20,10,0"
                      Height="400"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      Background="#FFFFFF"
                      Foreground="#1F2937"
                      BorderBrush="#E5E7EB"
                      BorderThickness="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="마켓이름"
                                       Binding="{Binding MarketName}"
                                       Width="350"
                                       IsReadOnly="True"/>
                    <DataGridComboBoxColumn x:Name="DeliveryCompanyColumn"
                                           Header="택배사"
                                           SelectedValueBinding="{Binding DeliveryCompany}"
                                           Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- DIVIDER 2 -->
            <Border Height="1" Background="#E5E7EB" Margin="10,20,10,0"/>

            <!-- BUTTONS -->
            <Grid Margin="10,20,10,20" Height="50">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="BtnGenerate"
                        Content="🔄 송장 생성"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="#1F2937"
                        Background="#F3F4F6"
                        Click="BtnGenerate_Click"
                        IsEnabled="False"
                        Grid.Column="0"
                        Cursor="Hand"/>

                <Button x:Name="BtnDownload"
                        Content="💾 Excel 다운로드"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="#1F2937"
                        Background="#F3F4F6"
                        Click="BtnDownload_Click"
                        IsEnabled="False"
                        Grid.Column="2"
                        Cursor="Hand"/>
            </Grid>

        </StackPanel>
    </Grid>

</Window>
